# SF UX Orchestrator v2 — Python dependencies

# Figma API + HTTP
requests>=2.31.0

# .env file loading
python-dotenv>=1.0.0

# Claude API — optional, used by ingestion agent for decision extraction
# and conflict detection. Falls back to rule-based extraction if absent.
anthropic>=0.25.0

# PDF transcript support — optional
# pip install pdfplumber
# pdfplumber>=0.10.0

# Word doc transcript support — optional
# pip install python-docx
# python-docx>=1.1.0
